name: Actions Diagnose

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print event
        run: |
          echo "event=${{ github.event_name }}"
          echo "sha=${{ github.sha }}"
